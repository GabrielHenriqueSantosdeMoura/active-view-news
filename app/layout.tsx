import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ActiveView News - Stay Informed",
  description: "Discover the latest news on your favorite topics. Search, save, and organize articles with ActiveView News.",
  keywords: ["news", "crypto", "NFT", "AI", "technology", "articles"],
  authors: [{ name: "ActiveView" }],
  openGraph: {
    title: "ActiveView News - Stay Informed",
    description: "Discover the latest news on your favorite topics.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
